The original Moodle module wwmoodle_set was replaced by wwmoodleset because 
the coding rules for Moodle request that all module names consist only
of alpha characters -- no numerals and no underscores.
(Detailed in the newmodule_template file:    

2. Change the name of the directory to your new module name.
   This name should be a single english word, if possible, 
   all lowercase and with only a-z characters. eg widget

A more substantial change is that the wwmoodleset calls to the webwork database use the
moodle defined ADODB database connection (global $db) rather than using a new database connection
(PEAR?) DB.pm.   

